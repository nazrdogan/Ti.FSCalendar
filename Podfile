# This is required for CocoaPods 1.x, which is recommended for Hyperloop
# If you still want to use the old CocoaPods, remove the following 2 lines
install! 'cocoapods',
         :integrate_targets => false

platform :ios, '10.0'
target 'Ti.FSCalendar' do
	  pod 'FSCalendar'
	
end